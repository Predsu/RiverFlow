codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: 30%
        threshold: 1%
    patch:
      default:
        target: 100%

comment:
  layout: "reach, diff, flags, files"
  behavior: default
